name: Release

on:
  push:
    tags:
      - 'v*'

env:
  MACCLOCK_SKIP_SNAPSHOTS: "1"

jobs:
  release:
    name: Build & publish release
    runs-on: macos-15
    timeout-minutes: 30
    permissions:
      contents: write
    steps:
      - name: Checkout (no external action — org policy is local_only)
        env:
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
        run: |
          curl -sL "https://github.com/${REPO}/archive/${SHA}.tar.gz" -o /tmp/src.tgz
          tar -xzf /tmp/src.tgz -C "$GITHUB_WORKSPACE" --strip-components=1

      - name: Show toolchain
        run: |
          sw_vers
          xcodebuild -version
          swift --version

      - name: Run tests (gate)
        run: swift test --parallel

      - name: Build .app bundle
        run: ./build-app.sh

      - name: Ad-hoc sign
        run: codesign --force --deep --sign - MacClock.app

      - name: Verify signature
        run: codesign --verify --verbose MacClock.app

      - name: Zip .app
        run: |
          ditto -c -k --keepParent MacClock.app MacClock.app.zip
          ls -lh MacClock.app.zip
          shasum -a 256 MacClock.app.zip

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
        run: |
          gh release create "$TAG" \
            --repo "$REPO" \
            --title "MacClock $TAG" \
            --generate-notes \
            MacClock.app.zip
